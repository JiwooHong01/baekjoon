#include <string>
#include <iostream>
#include <vector>
using namespace std;
typedef struct edge{
    string cur;
    edge
    int numchild = 0;
}node;

int main(void){
    int n, m;
    cin>>n>>m;
    
    node head;
    for(int i=0;i<(n+m);i++){
        string upfold, curfold;
        int flag;
        cin>>upfold>>curfold>>flag;
        if(strcmp(upfold, "main")== 0){
            
        }
    }
}